---
apiVersion: hub.traefik.io/v1alpha1
kind: API
metadata:
  name: checkin-api
  namespace: airlines
  labels:
    {{- include "airlines.labels" . | nindent 4 }}
spec:
  title: "Checkin API"
  description: "Boarding pass and seat assignment management"
  openApiSpec:
    path: /openapi.yaml
    override:
      servers:
        - url: {{ include "airlines.checkin.apiUrl" . }}
  cors:
    allowHeadersList: ["Authorization", "Content-Type"]
    allowOriginsList: ["*"]
    allowMethodsList: ["GET", "POST"]
    allowCredentials: true

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: checkin-route
  namespace: airlines
  labels:
    {{- include "airlines.labels" . | nindent 4 }}
    component: checkin
    bundle: trip
  annotations:
    hub.traefik.io/api: checkin-api
spec:
  entryPoints:
    {{- range .Values.entryPoints }}
    - {{ . }}
    {{- end }}
  routes:
    - kind: Rule
      match: {{ include "airlines.checkin.hostMatch" . }}
      services:
        - name: checkin-app
          port: 3000
