{{/*
  WRR TraefikService resources for HA routing.
  Rendered in parent mode when the group is assigned to a remote provider.
  Weights: local=3 (75%), remote=1 (25%).

  The remote leg uses kind: TraefikService with the internal @multicluster service name
  (format: <exposeName>-<providerKey>@multicluster). The local leg is a plain K8s service.

  Three HA services: flights-ha (flightOps), bookings-ha (passengerSvc), checkin-ha (airportOps).
  Other APIs in each group route directly to the uplink (no WRR).
*/}}
{{- if include "airlines.mc.isRemoteGroup" (dict "root" . "group" "flightOps") }}
{{- $ep := index .Values.multicluster.child.uplinkEntryPoints "flightOps" }}
{{- $provider := index .Values.multicluster.groups "flightOps" }}
---
apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: flights-ha
  labels:
    {{- include "airlines.labels" . | nindent 4 }}
    component: flights
spec:
  weighted:
    services:
      - name: flights-app
        namespace: {{ .Release.Namespace }}
        port: 3000
        weight: 3
      - kind: TraefikService
        name: {{ $ep }}@multicluster
        weight: 1
{{- end }}

{{- if include "airlines.mc.isRemoteGroup" (dict "root" . "group" "passengerSvc") }}
{{- $ep := index .Values.multicluster.child.uplinkEntryPoints "passengerSvc" }}
{{- $provider := index .Values.multicluster.groups "passengerSvc" }}
---
apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: bookings-ha
  labels:
    {{- include "airlines.labels" . | nindent 4 }}
    component: bookings
spec:
  weighted:
    services:
      - name: bookings-app
        namespace: {{ .Release.Namespace }}
        port: 3000
        weight: 3
      - kind: TraefikService
        name: {{ $ep }}@multicluster
        weight: 1
{{- end }}

{{- if include "airlines.mc.isRemoteGroup" (dict "root" . "group" "airportOps") }}
{{- $ep := index .Values.multicluster.child.uplinkEntryPoints "airportOps" }}
{{- $provider := index .Values.multicluster.groups "airportOps" }}
---
apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: checkin-ha
  labels:
    {{- include "airlines.labels" . | nindent 4 }}
    component: checkin
spec:
  weighted:
    services:
      - name: checkin-app
        namespace: {{ .Release.Namespace }}
        port: 3000
        weight: 3
      - kind: TraefikService
        name: {{ $ep }}@multicluster
        weight: 1
{{- end }}
