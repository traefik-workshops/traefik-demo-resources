apiVersion: v1
kind: Service
metadata:
  name: {{ include "presidio.fullname" . }}
  labels:
    {{- include "presidio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presidio.selectorLabels" . | nindent 4 }}
